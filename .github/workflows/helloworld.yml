name: "hello world workflow"

#controls when the actions will run. triggers the workflow on push some to master branch or production branch
on:
  push:
    branches:
        -master
        -production

jobs:
  hello_world:
    runs-on: ubuntu-latest
    steps:
     -  name: "print hello world"
        run: echo "hello world"